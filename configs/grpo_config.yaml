# GRPO Training Configuration

num_epochs: 3
batch_size: 2
gradient_accumulation_steps: 8
learning_rate: 5.0e-7
warmup_ratio: 0.1
lr_scheduler_type: cosine
logging_steps: 10
save_steps: 500
save_total_limit: 3
max_prompt_length: 512
max_completion_length: 512
num_generations: 4
temperature: 0.9
kl_coef: 0.05
run_name: grpo_alignment
